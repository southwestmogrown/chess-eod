const { expect } = require("chai");

describe.skip("Starter Week 1 Exercises (OOP + TDD)", () => {
  it("TODO: should display feedback for invalid selection", () => {
    expect.fail("Student exercise: write failing test first");
  });

  it("TODO: should switch turn after a legal move", () => {
    expect.fail("Student exercise: write failing test first");
  });

  it("TODO: should preserve board black-box usage in early tests", () => {
    expect.fail("Student exercise: avoid board internals in Week 1");
  });
});
